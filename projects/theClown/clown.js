
import * as THREE from 'three';
import { TW } from 'tw';


export function makeLeg(x, y, z) {}
export function makeArm(x, y, z) {}
export function makeHead(x, y, z) {}
export function makeBody(x, y, z) {}
export function makeClown(x, y, z) {}